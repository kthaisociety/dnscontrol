name: DNSControl push

on:
  push:
    branches: ["main"]
  workflow_dispatch:

concurrency:
  group: dnscontrol-apply
  cancel-in-progress: false

jobs:
  push:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    container:
      image: ghcr.io/dnscontrol/dnscontrol:5.2.0

    steps:
      - name: Checkout main
        uses: actions/checkout@v7

      - name: Push
        env:
          CLOUDFLARE_DNSCONTROL_TOKEN: ${{ secrets.CLOUDFLARE_DNSCONTROL_TOKEN }}
          MATTERMOST_WEBHOOK_URL: ${{ secrets.MATTERMOST_WEBHOOK_URL }}
        run: |
          rc=0
          dnscontrol push --notify --no-colors > push.log 2>&1 || rc=$?
          cat push.log
          exit $rc

      - name: Job summary
        if: always()
        run: |
          {
            echo '### DNSControl push'
            echo
            echo '```'
            cat push.log 2>/dev/null || echo '(push produced no output)'
            echo '```'
          } >> "$GITHUB_STEP_SUMMARY"
